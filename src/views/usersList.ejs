<% let pageTitle='Listado de usuarios' %>
    <%- include('partials/head', { pageTitle: pageTitle , styles: ['stylesInventory']}) %>

        <body>
            <%- include('./partials/header') %>
                <main>
                    <table>
                        <thead>
                            <th>Nombre</th>
                            <th>Apellido</th>
                            <th>Email</th>
                            <th>Rol</th>
                        </thead>
                        <tbody>
                            <% users.forEach(user=> { %>
                                <tr>
                                    <td>
                                        <%= user.first_name %>
                                    </td>
                                    <td>
                                        <%= user.last_name %>
                                    </td>
                                    <td>
                                        <%= user.email %>
                                    </td>
                                    <td>
                                        <%= user.roles?.description || 'No tiene rol' %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </main>
                <%- include('./partials/footer') %>
        </body>

        </html>