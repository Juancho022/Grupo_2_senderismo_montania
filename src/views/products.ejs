<% let pageTitle='Productos' %>
    <%- include('partials/head', { styles: ['products'], pageTitle: pageTitle }) %>

        <body>

            <%- include('./partials/header') %>

                <main>
                    <% locals.hiking && hiking.forEach(product => { %>
                    <article>
                        <a href="/products/<%= product.id %>"></a>
                        <div class="img-product">
                            <img src="/images/<%= product.image %>" class="img-product-products" alt="Imagen del Producto">
                            <div class="img-hover">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                        </div>
                        <div class="product-detail">
                            <h4><%= product.name %></h4>
                            <p class="price">$<%= product.price %></p>
                        </div>
                    </article>
                    <% }) %>
                    <% locals.climbing && climbing.forEach(product => { %>
                    <article>
                        <a href="/products/<%= product.id %>"></a>
                        <div class="img-product">
                            <img src="/images/<%= product.image %>" class="img-product-products" alt="Imagen del Producto">
                            <div class="img-hover">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                        </div>
                        <div class="product-detail">
                            <h4><%= product.name %></h4>
                            <p class="price">$<%= product.price %></p>
                        </div>
                    </article>
                    <% }) %>
                    <% locals.accessories && accessories.forEach(product => { %>
                    <article>
                        <a href="/products/<%= product.id %>"></a>
                        <div class="img-product">
                            <img src="/images/<%= product.image %>" class="img-product-products" alt="Imagen del Producto">
                            <div class="img-hover">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                        </div>
                        <div class="product-detail">
                            <h4><%= product.name %></h4>
                            <p class="price">$<%= product.price %></p>
                        </div>
                    </article>
                    <% }) %>
                    <% locals.footwear && footwear.forEach(product => { %>
                    <article>
                        <a href="/products/<%= product.id %>"></a>
                        <div class="img-product">
                            <img src="/images/<%= product.image %>" class="img-product-products" alt="Imagen del Producto">
                            <div class="img-hover">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                        </div>
                        <div class="product-detail">
                            <h4><%= product.name %></h4>
                            <p class="price">$<%= product.price %></p>
                        </div>
                    </article>
                    <% }) %>
                </main>

                <%- include('./partials/footer') %>

        </body>

        </html>